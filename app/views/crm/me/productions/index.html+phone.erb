<%=
  render(
    partial: 'production_cart',
    layout: 'card',
    collection: @productions,
    as: :model,
    locals: { cart: @cart, item_con: 'trade/agent/items', prod_con: controller_path }
  )
%>
