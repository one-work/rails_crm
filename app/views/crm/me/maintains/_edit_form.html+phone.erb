<%= form_with theme: 'default_phone', model: model, url: { action: 'update' } do |f| %>
  <%= render 'error_messages', target: f.object %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= f.submit %>
<% end %>
