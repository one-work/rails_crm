<div class="as-bar">
  <%= link_to({ controller: 'orders' }, class: 'px-4') do %>
    <%= svg_tag 'angle-left' %>
  <% end %>
  <span class="bar-title"><%= @client.name %></span>
  <i class="mx-5"></i>
</div>
