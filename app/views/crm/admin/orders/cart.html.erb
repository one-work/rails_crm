<%= render 'maintain_bar' %>

<ul class="breadcrumb">
  <li><%= link_to t('.productions.index.title'), { controller: 'productions' } %></li>
  <li class="is-active"><%= t('.title') %></li>
</ul>

<%= form_with url: { action: 'cart_create' }, id: 'new_order' do |f| %>
<% end %>

<%= render layout: 'index_table' do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @cart.checked_all_items, as: :model %>
<% end %>
