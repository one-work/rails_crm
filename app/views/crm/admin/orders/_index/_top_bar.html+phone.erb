<div class="as-bar">
  <% if current_state %>
    <%= link_to(current_state.referer, class: 'px-4') do %>
      <%= svg_tag 'angle-left', kind: 'regular' %>
    <% end %>
  <% else %>
    <%= link_to({ controller: 'contacts', action: 'show', id: @client.id }, class: 'px-4') do %>
      <%= svg_tag 'angle-left', kind: 'regular' %>
    <% end %>
  <% end %>
  <div class="bar-title">
    <span><%= @client.name %></span>
    <span class="px-2"></span>
    <span><%= t('.title') %></span>
  </div>
  <i class="px-5"></i>
</div>
