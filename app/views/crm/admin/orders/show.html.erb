<%= render 'maintain_bar' %>

<ul class="breadcrumb">
  <li><%= link_to t('.index.title'), { action: 'index' } %></li>
  <li class="is-active"><%= t('.title') %></li>
</ul>

<%= render 'index_actions' %>

<%= render 'show_table' %>
