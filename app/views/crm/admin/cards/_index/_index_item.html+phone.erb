<div class="is-several">
  <%= image_tag model.card_template.logo.variant(resize_to_limit: [24, 24]), class: 'image is-radius is-24x24' if model.card_template.logo.attached? %>
  <span><%= model.card_template.name %></span>
</div>
<div class="text-grey"><%= model.organ.name %></div>
