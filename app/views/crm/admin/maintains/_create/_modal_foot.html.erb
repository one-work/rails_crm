<div class="modal-card-foot justify-content-space-between">
  <%= button_to({ action: 'new_detect' }, params: { identity: params[:identity] }, class: 'button is-small is-link') do %>
    <%= svg_tag 'plus' %>
    <span class="ml-1"><%= t('.new_detect.title') %></span>
  <% end %>
  <%= submit_tag nil, form: 'client_form', class: 'button is-small is-primary' %>
</div>
